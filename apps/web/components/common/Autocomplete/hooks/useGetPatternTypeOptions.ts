export function useGetPatternTypeOptions() {
  const patternTypeOptions = [
    { label: "Weekly", value: "weekly" },

    { label: "Monthly", value: "monthly" },
  ];

  return { patternTypeOptions };
}
